it('{{ testDescription }}', function () {
    Saloon::fake([
        {{ requestClass }}::class => {{ mockResponse }},
    ]);

    $response = $this->{{ clientName }}->{{ resourceName }}()->{{ methodName }}({{ methodArguments }});

    Saloon::assertSent({{ requestClass }}::class);

    expect($response->status())->toBe(200);

    $dto = $response->dto();

    expect($dto)
{{ dtoAssertions }};
});
